# ── Meeting Copilot backend (pivoted from SME voice agent) ───────────────
# Python 3.11 / 3.12 on Windows.
#
# torch is intentionally NOT pinned here — install the build that matches your
# hardware BEFORE the rest (see README "GPU / CPU" section):
#   GPU (CUDA 12):  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CPU only:       pip install torch --index-url https://download.pytorch.org/whl/cpu

# API / server
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
python-multipart==0.0.20

# Gemini
google-genai==0.8.0

# Real-time STT (local, low-latency) — reused/upgraded from voice_engine_MVP
RealtimeSTT==0.3.104
faster-whisper==1.1.0

# Windows system-audio (WASAPI loopback) capture
PyAudioWPatch==0.2.12.7

# Audio utils
numpy==2.2.1
scipy==1.15.1
soundfile==0.13.0

# RAG (local embeddings, no external key)
chromadb==0.6.3
sentence-transformers==3.3.1
pypdf==5.1.0
python-docx==1.1.2

# ── Optional: TTS for "voice-agent" use cases (OFF in meeting mode) ──
# RealtimeTTS==0.4.55
# cartesia==1.4.0

# ── Legacy finance-bot deps (kept only if you still run the old streamlit_app) ──
# streamlit>=1.28.1 ; google-cloud-speech ; google-cloud-texttospeech ; redis
